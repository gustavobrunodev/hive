out
dist
pnpm-lock.yaml
LICENSE.md
tsconfig.json
tsconfig.*.json

# `npm run format` is `prettier --write .` — anything not listed here is fair
# game for a rewrite. These are ours to read, not ours to reformat; leaving them
# out once rewrote 200 files outside the change under way (STATE.md, 2026-07-27).
.claude
.specs
.scratch
coverage
# design-studio T2.2/T2.1: generated Design System bundle + derived catalog.
# `npm run build:ds-bundle` decides their formatting; a prettier pass would put
# the working tree permanently at odds with the generator's output.
resources/design-system-web-awesome
test-results
playwright-report
tools/visual
