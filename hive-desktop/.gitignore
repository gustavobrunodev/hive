node_modules
dist
dist-npm
out
coverage
.DS_Store
.eslintcache
*.log*
test-results
playwright-report
blob-report
playwright/.cache
.playwright-mcp

# Credentials never stage by accident. `.env.example` stays tracked as the
# documented shape.
.env
.env.*
!.env.example
